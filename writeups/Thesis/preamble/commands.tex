% !TEX root = ../main.tex
% Custom LaTeX commands for this thesis. (The CU Boulder template keeps these
% in its own macros.tex; ours live here so the template repo stays untouched.)

\newtheorem{theorem}{Theorem}[chapter]
